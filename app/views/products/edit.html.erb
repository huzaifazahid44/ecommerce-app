<div class="min-h-screen py-12">
  <div class="max-w-3xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="bg-gray-900 border border-gray-700 rounded-xl shadow-xl p-8 space-y-6">
      <div>
        <h2 class="text-2xl font-bold text-white">Edit Product</h2>
        <p class="text-sm text-gray-400 mt-1">Editing <span class="text-indigo-400"><%= @product.name %></span>.</p>
      </div>

      <%= render "form", product: @product %>
    </div>
  </div>
</div>
