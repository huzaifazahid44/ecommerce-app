<%= turbo_frame_tag "new_product" do %>
<div class="mb-8 bg-gray-900 border border-gray-700 rounded-xl shadow-xl p-8 space-y-6">
  <div>
    <h2 class="text-2xl font-bold text-white">New Product</h2>
    <p class="text-sm text-gray-400 mt-1">Fill in the details to add a new product to the catalog.</p>
  </div>

  <%= render "form", product: @product %>
</div>
<% end %>
